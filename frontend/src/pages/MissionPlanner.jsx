import { MissionPlanner } from "../components/Widgets/Mission";

const MissionsPlanner = ({ isSidebarOpen, darkMode }) => {
  return <MissionPlanner isSidebarOpen={isSidebarOpen} darkMode={darkMode} />;
};

export default MissionsPlanner;
